# Focus List

A client-side To-Do List application with full CRUD, delegated events, filtering, and automatic `localStorage` persistence.

Open `index.html` in a browser to use it. No build step or API key is required.
