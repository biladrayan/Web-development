# Skyline Weather Dashboard

A responsive asynchronous weather dashboard using the public Open-Meteo geocoding and forecast APIs. It demonstrates `fetch`, `async`/`await`, JSON parsing, search, loading state, and error handling.

Open `index.html` in a browser with an internet connection. No API key is needed.
